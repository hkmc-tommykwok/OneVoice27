# 把「加入你的身影」報名存到 Google 試算表

約 10 分鐘完成，全程免費，不需要伺服器。

## 第 1 步：建立試算表
1. 到 https://sheets.new 建立一份新的 Google 試算表（命名隨意，例如「同聲傳揚27 報名」）。
2. 複製網址中的試算表 ID：
   `https://docs.google.com/spreadsheets/d/`**`這一串就是ID`**`/edit`

## 第 2 步：貼上 Apps Script
1. 在試算表選單點「擴充功能 → Apps Script」。
2. 刪除預設內容，把專案裡 `google-apps-script.gs` 的全部內容貼上。
3. 把 `SPREADSHEET_ID` 換成第 1 步的 ID，按「儲存」。
4. （選做）執行一次 `testWrite` 函式，確認試算表出現「報名資料」分頁和一筆測試資料。第一次執行會要求授權，選你的 Google 帳號，看到「未驗證」警告時點「進階 → 前往（不安全）」繼續即可——這是你自己的腳本。

## 第 3 步：部署為 Web App
1. 右上角「部署 → 新增部署作業」。
2. 類型選「網頁應用程式」：
   - 執行身分：**我**（你的帳號）
   - 存取權：**所有人**
3. 點「部署」，複製結尾是 `/exec` 的 Web App URL。

## 第 4 步：接回網站
1. 打開 `src/config/registration.ts`，把 URL 貼進 `REGISTRATION_ENDPOINT`。
2. 重新建置並保存版本（告訴我 URL，我來做這步也行）。

完成後表單就會真正把資料寫入試算表；試算表欄位為：送出時間、光的類型、機構名稱、名字、姓氏、地址、電子郵件、同意接收電子報。

## 注意事項
- 在貼上 URL 之前，表單處於「展示模式」，只顯示成功訊息、不送出資料，頁面上也會有一行小字提示。
- Apps Script 免費配額對一般活動報名量綽綽有餘；若預期短時間湧入數千筆，再考慮改用後端方案。
- 「存取權：所有人」代表知道 URL 的人都能寫入；URL 只會寫在編譯後的程式碼裡，一般人看不出來，但請不要公開分享。若日後需要更嚴謹的保護（驗證碼、審核），告訴我即可加上。
